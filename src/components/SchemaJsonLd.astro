---
type Item = Record<string, unknown>;
interface Props { data: Item | Item[]; }
const { data } = Astro.props as Props;
const payload = Array.isArray(data) ? data : [data];
---
{payload.map((d) => (
  <script type="application/ld+json" set:html={JSON.stringify(d)} />
))}
